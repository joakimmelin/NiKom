service mybbs
{
    flags       = NAMEINARGS
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = nobody
    server      = /<path-to>/botcheck
    server_args = botcheck 1234 <IP or hostname> <port>
}
